export const getMenuButtonLabel = (openMenu: boolean) =>
  openMenu ? "Close menu" : "Open menu";

export const getMobileMenuVisibilityClass = (openMenu: boolean) =>
  openMenu ? "visible grid" : "invisible hidden";
